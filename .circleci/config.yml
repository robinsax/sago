version: 2
jobs:
  build:
    docker:
      - image: circleci/node:12.10
    working_directory: ~/repo
    steps:
      - checkout
      - run: sudo apt-get install postgresql && sudo npm i --save-dev -g
      - run: chmod +x ./bin/test-with-setup.sh
      - run: cat ./bin/test-with-setup.sh
